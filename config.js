/* Twin Bridge Estimator — runtime config.
   SOLAR_KEY: Google Maps Platform key with the Solar API enabled.
   Must be referrer-restricted to *.roofqc.com/*. Paste the key between the quotes.
   Leave empty to disable satellite auto-measure (users just set roof size by slider). */
window.SOLAR_KEY = "";
